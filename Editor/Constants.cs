namespace WarlordsRevengeEditor
{
    public static class Constants
    {
        public const float QUARTER = 0.25f;
        public const float HALF = 0.5f;
        public const float TWO_THIRDS = 2.0f / 3.0f;
        public const float THREE_QUARTERS = 0.75f;

        public const float HEX_WIDTH = 72.0f;
        public const float QUARTER_HEX_WIDTH = HEX_WIDTH * QUARTER;
        public const float HALF_HEX_WIDTH = HEX_WIDTH * HALF;
        public const float THREE_QUARTERS_HEX_WIDTH = HEX_WIDTH * THREE_QUARTERS;

        public const float HEX_HEIGHT = 72.0f;
        public const float HALF_HEX_HEIGHT = HEX_HEIGHT * HALF; 
    }
}